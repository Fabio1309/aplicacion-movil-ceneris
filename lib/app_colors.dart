// lib/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFD45B04); // Tu --title_color
  static const Color background = Color(0xFFF8F9FA); // Un gris claro
  static const Color text = Color(0xFF333333);
  static const Color textLight = Color(0xFF6C757D);
  static const Color card = Colors.white;
  // Tono cálido suave (melón) del card del login, para fondos que no
  // deben ser blanco puro pero tampoco compiten con el naranja de marca.
  static const Color melon = Color(0xFFFDEEE1);
}
